# Taxi Rank App

A starter Next.js application for managing taxi rank operations, authentication, rides, reviews, news, and QR-based workflows.

## Getting started

1. Install dependencies with `npm install`
2. Run the development server with `npm run dev`
3. Open http://localhost:3000
